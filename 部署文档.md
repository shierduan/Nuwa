# Nuwa 项目 Debian 服务器部署指南

## 概述

本指南介绍如何在 Debian 11/12 服务器上部署 Nuwa 项目，包含完整的环境配置、项目安装和命令行初始化方案。本部署方案暂时放弃 Live2D 和 TTS 功能，以确保项目能稳定运行在服务器环境中。

## 系统要求

- **CPU**: 至少 2 核
- **内存**: 至少 4 GB
- **存储**: 至少 20 GB 可用空间
- **操作系统**: Debian 11 (Bullseye) 或 Debian 12 (Bookworm)
- **网络**: 可访问 Internet 的服务器，用于下载依赖

## 1. 环境准备

### 1.1 更新系统

```bash
sudo apt update && sudo apt upgrade -y
```

### 1.2 安装必要工具

```bash
sudo apt install -y git wget curl python3-pip python3-venv build-essential
```

### 1.3 创建用户和工作目录

```bash
# 创建 nuwa 用户
sudo useradd -r -s /usr/sbin/nologin nuwa

# 创建工作目录
sudo mkdir -p /opt/nuwa
sudo chown -R $USER:$USER /opt/nuwa

# 进入工作目录
cd /opt/nuwa
```

## 2. 项目安装

### 2.1 克隆项目代码

```bash
git clone https://github.com/shierduan/Nuwa.git .
```

### 2.2 创建虚拟环境

```bash
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate

# 升级 pip
pip install --upgrade pip
```

### 2.3 安装依赖

```bash
pip install -r requirements.txt
```

### 2.4 配置系统

```bash
# 复制配置文件
cp config/config_example.yaml config/config.yaml

# 编辑配置文件（根据需要修改）
nano config/config.yaml
```

### 2.5 创建数据和日志目录

```bash
mkdir -p data logs monitoring/grafana/dashboards monitoring/grafana/datasources

# 设置权限
sudo chown -R nuwa:nuwa /opt/nuwa
```

## 3. 系统服务配置

### 3.1 创建 Systemd 服务文件

创建 `/etc/systemd/system/nuwa.service` 文件：

```ini
[Unit]
Description=Nuwa Core Service
After=network.target

[Service]
Type=simple
User=nuwa
Group=nuwa
WorkingDirectory=/opt/nuwa
Environment=PATH=/opt/nuwa/venv/bin
ExecStart=/opt/nuwa/venv/bin/python3 /opt/nuwa/main_async.py

# 进程管理
Restart=always
RestartSec=10
StartLimitInterval=300
StartLimitBurst=5

# 资源限制
LimitNOFILE=4096
LimitNPROC=2048

# 日志配置
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

### 3.2 启用和启动服务

```bash
sudo systemctl daemon-reload
sudo systemctl enable nuwa
sudo systemctl start nuwa
```

### 3.3 检查服务状态

```bash
sudo systemctl status nuwa
journalctl -u nuwa -f
```

## 4. 命令行管理工具

### 4.1 Nuwa 管理工具 (nuwactl.py)

项目提供了强大的 Python 管理工具 `nuwactl.py`，支持项目初始化、服务管理、配置编辑等功能。

#### 4.1.1 使用说明

```bash
python nuwactl.py [命令] [选项]
```

#### 4.1.2 命令列表

| 命令       | 功能描述                     | 使用说明                     |
|------------|------------------------------|------------------------------|
| init       | 初始化项目                   | 自动完成项目部署和配置       |
| start      | 启动 Nuwa 核心服务           | 启动系统服务                 |
| stop       | 停止 Nuwa 核心服务           | 停止系统服务                 |
| restart    | 重启 Nuwa 核心服务           | 重启系统服务                 |
| status     | 检查 Nuwa 核心服务状态       | 查看服务运行状态             |
| config edit| 编辑配置文件                 | 使用系统默认编辑器打开配置文件 |
| upgrade    | 升级项目到最新版本           | 自动备份、拉取更新、重启服务 |
| test       | 运行项目测试                 | 执行项目测试套件             |

#### 4.1.3 快速入门

```bash
# 初始化项目（包括安装依赖、创建配置、创建服务等）
python nuwactl.py init

# 启动服务
sudo python nuwactl.py start

# 检查服务状态
sudo python nuwactl.py status

# 停止服务
sudo python nuwactl.py stop

# 升级到最新版本
sudo python nuwactl.py upgrade
```

#### 4.1.4 管理工具特性

- **自动化部署**：一键完成项目初始化和部署
- **服务管理**：支持启动、停止、重启和状态检查
- **配置管理**：提供简单的配置文件编辑功能
- **数据备份**：升级时自动备份数据和配置
- **依赖管理**：自动处理 Python 依赖

### 4.2 传统初始化脚本 (init_nuwa.sh)

如果您偏好使用传统的 Shell 脚本，项目提供了 `init_nuwa.sh` 文件：

```bash
#!/bin/bash

# Nuwa 项目初始化脚本

set -e

echo "=== Nuwa 项目初始化 ==="

# 检查是否为 root 用户
if [ "$(id -u)" -eq 0 ]; then
    echo "请使用普通用户运行此脚本，sudo 权限将在需要时自动申请"
    exit 1
fi

# 检查操作系统
if [ ! -f /etc/debian_version ]; then
    echo "此脚本仅支持 Debian 系统"
    exit 1
fi

# 更新系统
echo "1. 更新系统..."
sudo apt update && sudo apt upgrade -y

# 安装依赖
echo "2. 安装依赖..."
sudo apt install -y git wget curl python3-pip python3-venv build-essential

# 创建用户和目录
echo "3. 配置用户和目录..."
if ! getent passwd nuwa > /dev/null; then
    sudo useradd -r -s /usr/sbin/nologin nuwa
fi

sudo mkdir -p /opt/nuwa
sudo chown -R $USER:$USER /opt/nuwa

cd /opt/nuwa

# 克隆项目
echo "4. 克隆项目..."
if [ -d .git ]; then
    echo "项目已存在，更新代码..."
    git pull
else
    git clone https://github.com/shierduan/Nuwa.git .
fi

# 创建虚拟环境
echo "5. 创建虚拟环境..."
if [ ! -d venv ]; then
    python3 -m venv venv
fi

# 激活虚拟环境并安装依赖
echo "6. 安装 Python 依赖..."
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt

# 配置系统
echo "7. 配置系统..."
mkdir -p data logs monitoring/grafana/dashboards monitoring/grafana/datasources

if [ ! -f config/config.yaml ]; then
    cp config/config_example.yaml config/config.yaml
    echo "已创建默认配置文件 config/config.yaml"
fi

# 设置权限
echo "8. 设置权限..."
sudo chown -R nuwa:nuwa /opt/nuwa

# 创建 Systemd 服务
echo "9. 创建系统服务..."
cat <<EOF | sudo tee /etc/systemd/system/nuwa.service > /dev/null
[Unit]
Description=Nuwa Core Service
After=network.target

[Service]
Type=simple
User=nuwa
Group=nuwa
WorkingDirectory=/opt/nuwa
Environment=PATH=/opt/nuwa/venv/bin
ExecStart=/opt/nuwa/venv/bin/python3 /opt/nuwa/main_async.py

Restart=always
RestartSec=10
StartLimitInterval=300
StartLimitBurst=5

LimitNOFILE=4096
LimitNPROC=2048

StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF

# 启用并启动服务
echo "10. 启动服务..."
sudo systemctl daemon-reload
sudo systemctl enable nuwa
sudo systemctl start nuwa

# 检查服务状态
echo "11. 检查服务状态..."
sudo systemctl status nuwa

echo -e "\n=== 初始化完成 ==="
echo "Nuwa 服务已成功启动"
echo -e "\n服务信息："
echo "  - 服务名称: nuwa"
echo "  - 状态检查: sudo systemctl status nuwa"
echo "  - 查看日志: journalctl -u nuwa -f"
echo "  - 重启服务: sudo systemctl restart nuwa"
echo "  - 停止服务: sudo systemctl stop nuwa"
```

### 4.3 运行初始化脚本

```bash
# 给脚本添加执行权限
chmod +x init_nuwa.sh

# 运行初始化脚本
./init_nuwa.sh
```

## 5. 监控配置（可选）

### 5.1 Prometheus 配置

创建 `/opt/nuwa/prometheus.yml` 文件：

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'nuwa'
    static_configs:
      - targets: ['localhost:8080']
    metrics_path: '/metrics'
    
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['localhost:9100']
```

### 5.2 Grafana 配置

创建 Grafana 数据来源配置文件 `/opt/nuwa/monitoring/grafana/datasources/datasource.yaml`：

```yaml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
```

## 6. 安全配置

### 6.1 防火墙配置

```bash
# 允许 SSH 和 Nuwa 服务
sudo ufw allow ssh
sudo ufw allow 8000/tcp  # HTTP API
sudo ufw allow 8001/tcp  # WebSocket
sudo ufw allow 8080/tcp  # 监控指标

# 启用防火墙
sudo ufw enable
sudo ufw status
```

### 6.2 SSL 加密（可选）

使用 Nginx 作为反向代理并配置 SSL：

```bash
sudo apt install nginx

# 安装 Certbot
sudo apt install certbot python3-certbot-nginx

# 配置证书
sudo certbot --nginx -d your-domain.com
```

## 7. 维护命令

### 7.1 启动/停止/重启

```bash
sudo systemctl start nuwa
sudo systemctl stop nuwa
sudo systemctl restart nuwa
```

### 7.2 查看日志

```bash
journalctl -u nuwa -f
```

### 7.3 检查服务状态

```bash
sudo systemctl status nuwa
```

### 7.4 更新项目

```bash
# 停止服务
sudo systemctl stop nuwa

# 进入项目目录
cd /opt/nuwa

# 激活虚拟环境
source venv/bin/activate

# 备份数据（可选）
cp -r data data.backup

# 拉取代码
git pull

# 安装新依赖（如果需要）
pip install -r requirements.txt

# 重启服务
sudo systemctl start nuwa
```

## 8. 故障排查

### 8.1 常见问题

1. **端口被占用**:
   ```bash
   lsof -i :8000
   kill -9 <PID>
   ```

2. **依赖问题**:
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt
   ```

3. **权限问题**:
   ```bash
   sudo chown -R nuwa:nuwa /opt/nuwa
   ```

### 8.2 调试模式

```bash
# 编辑 systemd 服务文件
sudo nano /etc/systemd/system/nuwa.service

# 添加调试环境变量
Environment=LOG_LEVEL=DEBUG

# 重启服务
sudo systemctl daemon-reload
sudo systemctl restart nuwa

# 查看详细日志
journalctl -u nuwa -f
```

## 9. 项目架构概述

### 9.1 文件结构

```
/opt/nuwa/
├── config/              # 配置文件
├── data/                # 数据存储
├── logs/                # 日志文件
├── monitoring/          # 监控配置
│   ├── grafana/         # Grafana 配置
│   └── prometheus/      # Prometheus 配置
├── nuwa_core/           # 核心代码
├── tests/               # 测试文件
├── venv/                # 虚拟环境
├── config.yaml          # 配置文件
├── health_check.py      # 健康检查
├── main_async.py        # 主入口
├── requirements.txt     # 依赖清单
└── server_async.py      # WebSocket 服务器
```

### 9.2 核心组件

- **NuwaKernelAsync**: 核心异步内核
- **NuwaState**: 状态管理
- **MemoryCortex**: 记忆皮层
- **DriveSystem**: 驱动系统
- **SemanticField**: 语义场
- **MemoryDreamer**: 记忆梦境

## 10. 版本控制

### 10.1 项目版本

```bash
# 查看项目版本
cat package.json
```

### 10.2 依赖版本

```bash
# 查看已安装依赖
pip list
```

## 11. 总结

本指南提供了完整的 Nuwa 项目部署方案，包括：
- 环境准备
- 项目安装
- 系统服务配置
- 命令行初始化
- 监控配置
- 安全配置
- 维护命令

按照此指南操作，您可以在 Debian 服务器上成功部署 Nuwa 项目，并通过 Systemd 服务进行管理。如果遇到问题，请参考故障排查部分。
